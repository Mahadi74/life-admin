<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme (Empty - Compose handles theming) -->
    <style name="Theme.LifeAdmin" parent="android:Theme.Material.Light.NoActionBar">
        <!-- Splash screen -->
        <item name="android:windowBackground">@color/splash_background</item>
        <item name="android:statusBarColor">@color/splash_background</item>
    </style>
</resources>
